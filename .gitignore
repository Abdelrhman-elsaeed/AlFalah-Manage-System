# .NET
bin/
obj/
*.user
*.suo
.vs/
*.DotSettings
*.DotSettings.user

# ASP.NET Core
appsettings.Development.json
appsettings.Production.json
!appsettings.json

# EF Core migrations temp
*.migration.sql

# Node / Angular
# These used to point at frontend/al-falah-app/, a path that does not exist —
# the app lives directly under frontend/, so nothing was actually ignored.
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.angular/

# Secrets (never commit)
*.pfx
secrets/
*.pem
**/App_Data/DataProtectionKeys/
# Live-testing JWTs. Four of these were committed by mistake; they are removed
# from the tree but remain in git history, so the signing key must be rotated.
*.tok

# Logs — a 6 MB backend.log was tracked and churned on every run.
*.log
logs/

# Local scratch build output
.obj-check/
.build-check/

# OS
.DS_Store
Thumbs.db

# Local audit / QA artefacts (contain JWT tokens and IDs from live testing — never commit)
.audit/
nul
backend/nul
frontend/nul
